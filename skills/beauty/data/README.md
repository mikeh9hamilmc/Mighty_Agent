# Beauty Data Directory

Place the user's beauty routines, product lists, or records in this folder.
- Supported formats: `.pdf`, `.docx`, `.xlsx`, `.md`, `.txt`
- The system will automatically convert `.pdf` and `.docx` into `.md` text representations for fast searching.
- The `beauty` agent can query these files using `grep_documents` and `view_document`.
