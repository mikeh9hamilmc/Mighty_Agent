# Beauty Memory Directory

This folder holds persistent Markdown notes created by the `beauty` sub-agent.
- The agent uses `save_memory` to write notes here and `read_memory` to access them.
- Useful for remembering the user's skin type, allergies, or long-term anti-aging strategies.
