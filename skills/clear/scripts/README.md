# scripts/

This skill is handled natively by the bot via the `/clear` command in `src/bot.js`.
No Python script is required — the bot calls `session.clear()` directly.

This folder exists for structural consistency with other skills.
